{
  "name": "marcus-arcade",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/verify-games.js",
    "sync-daily": "node scripts/daily-sync.js",
    "pipeline": "npm test && npm run sync-daily && npm run build"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
